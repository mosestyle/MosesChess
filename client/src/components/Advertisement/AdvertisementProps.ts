import { CSSProperties } from "react";

interface AdvertisementProps {
    className?: string;
    style?: CSSProperties;
    publisherId?: string;
    adUnitId: string;
}

export default AdvertisementProps;